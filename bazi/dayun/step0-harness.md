<!-- ORCHESTRATOR METADATA
step: 0
reasoning: 🟢轻（数据提取，无推理）
gate_check: |
  ASSERT 四柱干支 != null（4组）
  ASSERT 用神 != null
  ASSERT 喜忌仇闲 != null（4个）
  ASSERT 格局名称 != null
  ASSERT 调候用神 != null
  ASSERT 大运列表.length >= 6
  ASSERT 地支关系扫描已完成
next_step: 1
-->

# Step 0：前序数据提取 -- 执行指令

> 前置：Phase 0-5 已全部完成
> 输入：Phase 0-5 所有卡片输出
> 输出：前序数据卡（下方模板）
> 原则：**纯数据搬运，不做任何新判断**。每项数据标注来源 Phase。

---

## 0.1 从 Phase 0（排盘数据卡）提取 🟢查表

```
操作：打开 Phase 0 排盘数据卡，逐项提取：

→ 四柱干支：
  年柱 = [天干][地支]
  月柱 = [天干][地支]
  日柱 = [天干][地支]（日干 = 命主元素）
  时柱 = [天干][地支]

→ 日主五行 = [日干的五行属性]
→ 日主阴阳 = [阳干/阴干]

→ 四柱藏干：
  年支藏干 = [本气/中气/余气]
  月支藏干 = [本气/中气/余气]（月令本气 = 月令主气）
  日支藏干 = [本气/中气/余气]
  时支藏干 = [本气/中气/余气]

→ 四柱十神标注：
  年干十神 = [X]
  月干十神 = [X]
  日干十神 = 日主
  时干十神 = [X]

→ 空亡 = [X, Y]（日柱查旬空）

→ 大运列表（至少6步）：
  第1步：[干支]（[起运岁]~[结束岁]）
  第2步：[干支]（[X]~[Y]）
  ...（逐步列完）

→ 性别 = [男/女]
→ 起运岁数 = [X]岁
→ 神煞 = [已标注的神煞列表]
```

---

## 0.2 从 Phase 1（五行统计卡）提取 🟢查表

```
操作：打开 Phase 1 输出卡片，提取：

→ 五行统计（天干+地支藏干合计）：
  木 = [X]个
  火 = [X]个
  土 = [X]个
  金 = [X]个
  水 = [X]个

→ 气候判定 = [寒/热/燥/湿/中和]

→ 地支关系（Phase 1 已识别的）：
  六冲 = [有/无]，具体：[X冲Y]
  六合 = [有/无]，具体：[X合Y]
  三合 = [有/无]，具体：[XYZ三合]
  三会 = [有/无]，具体：[XYZ三会]
  三刑 = [有/无]，具体：[XYZ三刑]
  六害 = [有/无]，具体：[X害Y]
  半合 = [有/无]，具体：[XY半合]
```

---

## 0.3 从 Phase 2（旺衰判定卡）提取 🟢查表

```
操作：打开 Phase 2 输出卡片，提取：

→ 旺衰六级判定 = [旺极/太旺/偏旺/中和/偏弱/太弱/弱极/从格]
→ 旺衰依据 = [得令/失令 + 得地/失地 + 得势/失势]

→ 格局名称 = [X格]
→ 格局成败 = [成格/破格/半成半破]
→ 格局层次 = [上等/中等/下等]

→ 调候满足度 = [充分满足/部分满足/不满足]
→ 调候用神 = [X]（来自穷通宝鉴查表）
```

---

## 0.4 从 Phase 3（用神体系卡）提取 🟢查表

```
操作：打开 Phase 3 输出卡片，提取：

→ 用神 = [X]（五行+十神）
→ 喜神 = [X]
→ 忌神 = [X]
→ 仇神 = [X]
→ 闲神 = [X]

→ 病药结构 = [病：X / 药：Y]
→ 取用法 = [扶抑/调候/通关/专旺/从格]

→ 十神组合关键信号：
  [列出 Phase 3 已识别的十神组合，如：杀印相生/伤官佩印/食神制杀/官杀混杂 等]
```

---

## 0.5 从 Phase 4（逐柱关系卡）提取 🟢查表

```
操作：打开 Phase 4 输出卡片，提取：

→ 盖头截脚记录：
  年柱 = [干克支/支克干/干生支/支生干/干支一气/无特殊关系]
  月柱 = [同上]
  日柱 = [同上]
  时柱 = [同上]

→ 天干合冲记录：
  [列出所有天干间的合/冲/克关系]
```

---

## 0.6 从 Phase 5（地支关系全扫描卡）提取 🟢查表

```
操作：打开 Phase 5 输出卡片，提取：

→ 地支关系完整清单（Phase 5 全扫描结果）：
  六冲 = [完整列表+胜败判定]
  三合 = [完整列表+是否成局]
  三会 = [完整列表+是否成局]
  六合 = [完整列表+合化/合绊判定]
  三刑 = [完整列表+引动状态]
  六害 = [完整列表]
  半合 = [完整列表+类型]
  暗合 = [完整列表]
  伏吟 = [有/无]
  反吟 = [有/无]
  空亡状态 = [真空/假空+涉及地支]
  墓库状态 = [入墓/入库+涉及地支]
```

---

## Step 0 输出模板

```
━━━━━━━━━━ 前序数据卡 ━━━━━━━━━━

【四柱】（来源：Phase 0）
年柱：[干][支]  月柱：[干][支]  日柱：[干][支]  时柱：[干][支]
日主：[干]（[五行][阴阳]）
空亡：[X, Y]

【藏干】（来源：Phase 0）
年支：[本气/中气/余气]
月支：[本气/中气/余气]（月令本气 = [X]）
日支：[本气/中气/余气]
时支：[本气/中气/余气]

【十神】（来源：Phase 0）
年干[X] = [十神]  月干[X] = [十神]  时干[X] = [十神]

【五行统计】（来源：Phase 1）
木[X] 火[X] 土[X] 金[X] 水[X]
气候：[X]

【旺衰与格局】（来源：Phase 2）
旺衰 = [六级判定]  依据 = [得令/失令 + 得地/失地 + 得势/失势]
格局 = [格局名]  成败 = [成格/破格]  层次 = [X等]
调候 = [用神]  满足度 = [X]

【五神体系】（来源：Phase 3）
用神 = [X]  喜神 = [X]  忌神 = [X]  仇神 = [X]  闲神 = [X]
取用法 = [X]  病药 = [病X / 药Y]
十神组合信号 = [列表]

【逐柱关系】（来源：Phase 4）
盖头截脚 = [逐柱记录]
天干合冲 = [记录]

【地支关系】（来源：Phase 5）
六冲 = [X]  六合 = [X]  三合 = [X]  三会 = [X]
三刑 = [X]  六害 = [X]  半合 = [X]
暗合 = [X]  伏吟 = [X]  反吟 = [X]
空亡状态 = [真空/假空]  墓库状态 = [入墓/入库]

【大运列表】（来源：Phase 0）
性别：[男/女]  起运：[X]岁
第1步：[干支]（[X~Y]岁）
第2步：[干支]（[X~Y]岁）
...
（至少6步）

━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Step 0 Gate Check

```
ASSERT 四柱干支 != null（4组天干+地支全部填写）
ASSERT 日主五行 != null AND 日主阴阳 != null
ASSERT 藏干 != null（4个地支的藏干全部填写）
ASSERT 用神 != null AND 用神 IN [木,火,土,金,水]
ASSERT 喜神 != null AND 忌神 != null AND 仇神 != null AND 闲神 != null
ASSERT 格局名称 != null AND 格局名称 != ""
ASSERT 调候用神 != null
ASSERT 大运列表.length >= 6
ASSERT 旺衰判定 IN [旺极,太旺,偏旺,中和,偏弱,太弱,弱极,从格]
ASSERT 地支关系.六冲检查 == done
ASSERT 地支关系.六合检查 == done
ASSERT 地支关系.三合检查 == done
ASSERT 地支关系.三刑检查 == done

→ 全部通过 → 进入 Step 1
→ 任一不通过 → 标注缺失项，回查对应 Phase 补齐后重检
```

---

## 回溯触发条件

```
回溯路径: Step 0 → Phase 0-5（对应缺失项的 Phase）
触发条件: Gate Check 中任一 ASSERT 失败
执行: 回到缺失数据所属的 Phase，补齐后重新提取
上限: 每个缺失项回溯1次
```
